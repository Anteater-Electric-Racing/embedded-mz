{
    "name": "Rust",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "curl https://www.influxdata.com/d/install_influxdb3.sh | bash && sudo cp .devcontainer/influxdb3 /etc/init.d/influxdb3 && sudo chmod +x /etc/init.d/influxdb3 && sudo service influxdb3 start && (sudo service grafana-server start || true)",
    "forwardPorts": [
        8181,
        3000
    ]
}